import styled from 'styled-components';

/**
 * Heatmap container component
 */
const HeatmapContainer = styled.div`
  display: grid;
  grid-template-columns: repeat(7, 1fr);
  gap: 4px;
  margin-top: 16px;
`;

export default HeatmapContainer; 